<h1>Posts#index</h1>

<% @posts.each do |post| %>
	<p><%= post.title %></p>
	<p><%= post.body %></p>
	<%= link_to 'edit', edit_post_path(post) %>
	<%= link_to 'destroy', post_path(post), :method => :delete, :confirm => 'Are you sure?' %>
<% end %>